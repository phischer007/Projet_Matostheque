export const quickNotifyOption = {
    // Automatically close the notification after 2000 milliseconds (2 seconds)
    autoClose: 2000,

    // Hide the progress bar in the notification
    hideProgressBar: true,

    // Allow the user to close the notification by clicking on it
    closeOnClick: true,

    // Make the notification draggable by the user
    draggable: true,

    // Do not display a progress indicator in the notification
    progress: undefined
};